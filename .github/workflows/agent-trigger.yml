name: Agent Trigger

on:
  issue_comment:
    types: [created]
  pull_request:
    types: [opened, reopened, ready_for_review]

jobs:
  forward:
    uses: yidongw/discord-ai-terminal/.github/workflows/agent-trigger.yml@main
    secrets: inherit
