name: Integration Tests (real DB)

# Runs the real-database integration suites on `dev`:
#   - packages/database: migrations + seed + seed_company + seedDemoData
#   - apps/erp: service functions over real PostgREST (supabase-js)
# Gated to `dev` so broken migrations/APIs are caught before staging, and to the
# relevant paths so unrelated PRs don't pay the cost.

on:
  pull_request:
    branches: [dev]
    paths:
      - "packages/database/**"
      - "apps/erp/app/**"
      - ".github/workflows/integration-tests.yml"
  push:
    branches: [dev]
    paths:
      - "packages/database/**"
      - "apps/erp/app/**"
      - ".github/workflows/integration-tests.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # Same images as the harness defaults (packages/database/src/test/dockerHarness.ts),
  # but pulled from Docker Hub instead of ECR Public: ECR Public rate-limits
  # anonymous pulls hard (shared runner IPs → "toomanyrequests"), and a failed
  # first pull never populates the cache. The harness reads these overrides; the
  # workflow also uses them as cache keys.
  ITEST_POSTGRES_IMAGE: supabase/postgres:15.8.1.085
  ITEST_POSTGREST_IMAGE: postgrest/postgrest:v14.10

jobs:
  integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - uses: actions/cache@v4
        with:
          path: |
            node_modules
            apps/*/node_modules
            packages/*/node_modules
          key: node-modules-${{ hashFiles('pnpm-lock.yaml') }}

      - run: pnpm install --frozen-lockfile

      # Cache the (large) Docker images as gzipped `docker save` tarballs so we
      # don't re-pull them every run. Keyed to the image tags, so bumping a tag
      # naturally busts the cache.
      - name: Cache Docker images
        id: docker-image-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/itest-images
          key: itest-images-${{ env.ITEST_POSTGRES_IMAGE }}-${{ env.ITEST_POSTGREST_IMAGE }}

      - name: Load or pull Docker images
        run: |
          set -euo pipefail
          DIR="$HOME/.cache/itest-images"
          mkdir -p "$DIR"
          pull_with_retry() {
            local image="$1" attempts=6 delay=10 i
            for i in $(seq 1 "$attempts"); do
              if docker pull "$image"; then return 0; fi
              echo "pull of $image failed (attempt $i/$attempts); retrying in ${delay}s…"
              sleep "$delay"
              delay=$((delay + 15))
            done
            echo "giving up on $image after $attempts attempts"
            return 1
          }
          load_or_pull() {
            local image="$1" tar="$2"
            if [ -f "$tar" ]; then
              echo "Loading cached image from $tar"
              gunzip -c "$tar" | docker load
            else
              echo "Pulling $image"
              pull_with_retry "$image"
              docker save "$image" | gzip > "$tar"
            fi
          }
          load_or_pull "$ITEST_POSTGRES_IMAGE" "$DIR/postgres.tar.gz"
          load_or_pull "$ITEST_POSTGREST_IMAGE" "$DIR/postgrest.tar.gz"

      - name: Database integration tests
        run: pnpm --filter @carbon/database test:integration

      - name: ERP API integration tests
        run: pnpm --filter erp test:integration
