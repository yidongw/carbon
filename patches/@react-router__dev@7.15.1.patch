diff --git a/dist/vite.js b/dist/vite.js
--- a/dist/vite.js
+++ b/dist/vite.js
@@ -1281,10 +1281,12 @@
 async function write(...files) {
-  return Promise.all(
-    files.map(async ({ filename: filename2, content }) => {
-      await import_promises.default.mkdir(Path4.dirname(filename2), { recursive: true });
-      await import_promises.default.writeFile(filename2, content);
-    })
-  );
+  const dirs = [...new Set(files.map(({ filename: filename2 }) => Path4.dirname(filename2)))];
+  for (const dir of dirs) {
+    await import_promises.default.mkdir(dir, { recursive: true });
+  }
+  return Promise.all(
+    files.map(({ filename: filename2, content }) =>
+      import_promises.default.writeFile(filename2, content)
+    )
+  );
 }
diff --git a/dist/cli/index.js b/dist/cli/index.js
index da54c0d..e51d41f 100644
--- a/dist/cli/index.js
+++ b/dist/cli/index.js
@@ -1654,16 +1654,19 @@ async function cleanViteManifests(environmentsOptions, ctx) {
   );
   await Promise.all(
     viteManifestPaths.map(async (viteManifestPath) => {
-      let manifestExists = (0, import_node_fs3.existsSync)(viteManifestPath);
-      if (!manifestExists) return;
-      if (!ctx.viteManifestEnabled) {
-        await (0, import_promises3.rm)(viteManifestPath, { force: true, recursive: true });
-      }
-      let viteDir = path8.dirname(viteManifestPath);
-      let viteDirFiles = await (0, import_promises3.readdir)(viteDir, { recursive: true });
-      if (viteDirFiles.length === 0) {
-        await (0, import_promises3.rm)(viteDir, { force: true, recursive: true });
-      }
+     try {
+        let manifestExists = (0, import_node_fs3.existsSync)(viteManifestPath);
+        if (!manifestExists) return;
+        if (!ctx.viteManifestEnabled) {
+          await (0, import_promises3.rm)(viteManifestPath, { force: true, recursive: true });
+        }
+        let viteDir = path8.dirname(viteManifestPath);
+        if (!(0, import_node_fs3.existsSync)(viteDir)) return;
+        let viteDirFiles = await (0, import_promises3.readdir)(viteDir, { recursive: true });
+        if (viteDirFiles.length === 0) {
+          await (0, import_promises3.rm)(viteDir, { force: true, recursive: true });
+        }
+     } catch {}
     })
   );
 }
