{
  "enabledPlugins": {
    "superpowers@claude-plugins-official": true
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); echo \"$INPUT\" | jq -r '.command // \"\"' | grep -q 'gh pr create' && { echo 'PR creation is disabled. Commit and push the branch, then stop. Ask the user explicitly if they want a PR.'; exit 2; } || true"
          }
        ]
      }
    ]
  }
}
